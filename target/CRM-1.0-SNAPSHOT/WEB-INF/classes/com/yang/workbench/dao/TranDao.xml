<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yang.workbench.dao.TranDao">
    <insert id="save">
        insert into tbl_tran(
        id,
        owner,
        money,
        name,
        expectedDate,
        customerId,
        stage,
        type,
        source,
        activityId,
        contactsId,
        createBy,
        createTime,
        description,
        contactSummary,
        nextContactTime

        )
        values(
        #{id},
        #{owner},
        #{money},
        #{name},
        #{expectedDate},
        #{customerId},
        #{stage},
        #{type},
        #{source},
        #{activityId},
        #{contactsId},
        #{createBy},
        #{createTime},
        #{description},
        #{contactSummary},
        #{nextContactTime}

        )
    </insert>

    <select id="detail" resultType="Tran">
        select
        tran.id,
        user.name owner,
        tran.money,
        tran.name,
        tran.expectedDate,
        cus.name customerId,
        tran.stage,
        tran.type,
        tran.source,
        activity.name activityId,
        contacts.fullname contactsId,
        tran.createBy,
        tran.createTime,
        tran.editBy,
        tran.editTime,
        tran.description,
        tran.contactSummary,
        tran.nextContactTime


         from tbl_tran tran
         join tbl_user user
         on tran.owner = user.id
         join tbl_customer cus
         on tran.customerId = cus.id
         left join tbl_activity activity
         on tran.activityId = activity.id
         left join tbl_contacts contacts
         on tran.contactsId = contacts.id
        where tran.id=#{id}
    </select>
  	
</mapper>

































